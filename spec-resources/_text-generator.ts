import * as textGen from "./generator-foobar";

// This script generates some random text that is compatible with
// the mock tokenizer.

// From this directory, run the command:
// `npx ts-node ./_text-generator.ts`

console.log(textGen.generateText());